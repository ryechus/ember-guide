import { rentalType } from '../../../helpers/rental-type';
import { module, test } from 'qunit';

module('Unit | Helper | rental type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rentalType(42);
  assert.ok(result);
});
